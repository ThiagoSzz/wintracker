import { createUseStyles } from 'react-jss';

export const useUserInputStyles = createUseStyles({
  formGroup: {
    marginBottom: '1rem',
  },
  textInput: {
    marginBottom: '1rem',
  },
  errorAlert: {
    marginBottom: '1rem',
  },
});